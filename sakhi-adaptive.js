window.SakhiAdaptive=(function(){
'use strict';
var PLACEMENT={reading:['reading.short_vowels','reading.cvc_mixed','reading.oral_blending'],math:['math.compare','math.add_5','math.count_20'],writing:['writing.cvc_words','writing.letter_lowercase'],language:['language.listening','language.wh_questions'],logic:['logic.patterns','logic.classify'],science:['science.observe','science.habitats'],wellbeing:['wellbeing.emotions'],creative:['creative.imaginative_play']};
function attempts(domain){return(SakhiProgress.load().attempts||[]).filter(function(a){return a.domain_id===domain&&a.objective;});}
function score(sk,used){var e=SakhiProgress.evidence(sk.skill_id),s=0;if(used.indexOf(sk.skill_id)>=0)s-=100;if(e.next_review&&Date.parse(e.next_review)<=Date.now())s+=55;if(e.mastery_state==='LEARNING')s+=36;if(e.mastery_state==='DEVELOPING')s+=30;if(e.mastery_state==='NOT_INTRODUCED')s+=24;if(e.last_accuracy!==null&&e.last_accuracy<.65)s+=20;if(e.mastery_state==='MASTERED')s-=30;s-=Math.min(15,e.attempt_count||0);return s;}
function pick(domain,used){used=used||[];var seen=attempts(domain);if(seen.length<2){var checks=(PLACEMENT[domain]||[]).map(function(id){return SakhiCurriculum.skill(id);}).filter(Boolean).filter(function(s){return used.indexOf(s.skill_id)<0;});if(checks.length){var p=checks[Math.min(seen.length,checks.length-1)];return{skill_id:p.skill_id,band:Math.max(2,SakhiProgress.bandFor(p.skill_id)),reason:'quick placement check',placement:true};}}
 var candidates=SakhiCurriculum.frontier(domain,SakhiProgress.masteryOf).concat(SakhiCurriculum.skillsIn(domain)).filter(function(x,i,a){return a.findIndex(function(y){return y.skill_id===x.skill_id;})===i;}).sort(function(a,b){return score(b,used)-score(a,used);}),best=candidates[0];if(!best)return null;var e=SakhiProgress.evidence(best.skill_id),reason=e.next_review&&Date.parse(e.next_review)<=Date.now()?'spaced review':e.last_accuracy!==null&&e.last_accuracy<.65?'targeted support':e.success_streak>=1?'ready for a harder challenge':'best next step';return{skill_id:best.skill_id,band:SakhiProgress.bandFor(best.skill_id),reason:reason};}
function explain(p){if(!p)return'Practice complete for now.';var s=SakhiCurriculum.skill(p.skill_id);return s.title+' — '+p.reason+'.';}
return{pick:pick,explain:explain};
})();
