const fs=require('fs');
const files={contract:fs.readFileSync('activity-contract.js','utf8'),engine:fs.readFileSync('interaction-engine.js','utf8'),progress:fs.readFileSync('progress-service.js','utf8'),reward:fs.readFileSync('reward-service.js','utf8'),persistence:fs.readFileSync('persistence.js','utf8'),css:fs.readFileSync('runtime-stability.css','utf8'),speech:fs.readFileSync('speech-service.js','utf8'),phoneme:fs.readFileSync('phoneme-audio.js','utf8')};
const fail=[];
for(const field of ['activity_id','session_id','domain','skill_id','difficulty','interaction_type','learning_objective','instruction_text','spoken_instruction','character_prompt','items','targets','correct_answer','distractors','hint_1','hint_2','hint_3','success_feedback','retry_feedback','mastery_signal','estimated_minutes','visual_assets','audio_assets','reward_rules','completion_rules'])if(!files.contract.includes(`'${field}'`))fail.push(`contract missing ${field}`);
for(const type of ['tap_choice','multi_select','drag_drop','sort','match','memory','sequence','word_builder','number_manipulative','pattern_builder','find_it','trace','story_choice','movement','offline_activity','creative_activity'])if(!files.engine.includes(`${type}:`))fail.push(`renderer registry missing ${type}`);
for(const state of ['LOADING','READY','IN_PROGRESS','HINTED','COMPLETED','SAVING','SAVED','ERROR'])if(!files.contract.includes(`'${state}'`))fail.push(`state machine missing ${state}`);
for(const event of ['ACTIVITY_SCHEMA_INVALID','ACTIVITY_RENDER_ZERO_SIZE','ACTIVITY_RENDER_FAILED','ACTIVITY_COMPLETED','ATTEMPT_SAVE_STARTED','ATTEMPT_SAVE_SUCCESS','ATTEMPT_SAVE_FAILED','MASTERY_UPDATED'])if(!(files.engine+files.progress+files.contract).includes(event))fail.push(`runtime log missing ${event}`);
if(/function\s+makeQuest\s*\(/.test(files.engine))fail.push('interaction engine still owns lesson planning');if(/data\.shinyStars\s*\+\+/.test(files.engine))fail.push('renderer directly increments reward balance');
if(!files.progress.includes('saveAttempt')||!files.progress.includes('completeSession')||!files.progress.includes('refreshAuthoritative'))fail.push('ProgressService authoritative completion API incomplete');
if(/recordForActivity\s*\(/.test(files.progress))fail.push('ProgressService still performs a second reward write');
if(!files.reward.includes("authoritativeIssuer:'SUPABASE_COMPLETE_ACTIVITY'"))fail.push('RewardService is not an authoritative ledger projection');
if(!files.persistence.includes("rpc('complete_activity'")||!files.persistence.includes("rpc('get_learner_snapshot'"))fail.push('persistence must use atomic completion + authoritative snapshot RPCs');
if(/from\(['"]learning_attempts['"]\).*insert|from\(['"]reward_transactions['"]\).*insert/.test(files.persistence))fail.push('persistence contains fragile direct attempt/reward writes');
if(!files.engine.includes('traceMetrics')||!files.engine.includes('completion_threshold')||!files.engine.includes('minimum_stroke_points'))fail.push('TraceCanvas uses weak touch-count completion');
if(!files.css.includes('#interactionMount')||!files.css.includes('min-height:240px')||!files.css.includes('--z-activity'))fail.push('activity visibility/layer contract missing');
if(!files.speech.includes('PhonemeAudioService?.play')||/kind\s*===\s*['"]phoneme['"]/.test(files.speech))fail.push('phoneme path is not isolated from narration');
for(const id of ['t','p'])if(!files.phoneme.includes(`phoneme_${id}.ogg`))fail.push(`critical phoneme ${id} missing local asset`);
if(fail.length){console.error('Runtime stability validation failed:\n- '+fail.join('\n- '));process.exit(1)}
console.log('Runtime stability validation passed: canonical renderer, atomic save/mastery/reward flow, real trace evidence, local critical phonemes and visible activity shell.');
