const fs=require('fs'),path=require('path');const root=path.join(__dirname,'..');const p=JSON.parse(fs.readFileSync(path.join(root,'data/six-month-plan.json'),'utf8'));let days=0;for(const w of p.weeks){if(w.days.length!==5)throw new Error('Week not 5 days');for(const d of w.days){days++;if(d.total_minutes!==30)throw new Error('Bad minutes');const core=d.missions.map(m=>m.domain);for(const x of ['reading','math','writing'])if(!core.includes(x))throw new Error('Missing core '+x);if(d.offscreen.minutes<4)throw new Error('No offscreen block');}}console.log(`Plan passed: ${p.weeks.length} weeks / ${days} learning days`);
